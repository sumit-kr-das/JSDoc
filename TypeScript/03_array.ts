let arr = ["Sumit", "Supu", "Dipali"];
console.log(arr);

/*  
    we can push only string values,
    because the array contains only string value.
    if the conntails string,number,bool value,
    so we can push string,number,bool values 
    like: let arr = ["sumit",55,true]    
*/

/* type custing in array */

let data:string[] = ['anjali','jaya','sutapa'];
data.push("sumita");
console.log(data);

/* Here we can push only string value */
